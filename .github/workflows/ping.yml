name: Keep Render Alive

on:
  schedule:
    - cron: "*/10 * * * *"  # 10분마다 실행
  workflow_dispatch:        # 수동 실행도 가능

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render service
        run: |
          curl -I https://backend-xhrb.onrender.com
          curl -I https://admin-7g9o.onrender.com
          curl -I https://analysis-3f15.onrender.com